#include <string>
#include <iostream>
#include <map>
#include <vector>
#include <stdio.h>
#include <utility>
#include <assert.h>

using std::string; 
using std::cin;
using std::cout;
using std::endl;
using std::map;
using std::vector;

//auto-gen != >= > <= if == and < are defined
using namespace std::rel_ops;
